# 第2次提示词记录

## 本次任务重点

在项目根目录新建独立 `reader-workbench/` 模块，用于开发和部署 Moonlight-like 三栏阅读器思路，并保留后续接入 `LLM Wiki` 的接口边界。

## 下一步行动

1. 运行 `reader-workbench` 本地前端，完成浏览器可视检查。
2. 将真实 PDF/Markdown 解析成 `page_id/block_id` artifacts。
3. 在后端新增 `/api/reader/*` 路由，不破坏现有 intake/workbench API。
4. 把 AI 解释产出的候选概念接入现有人工审核和 accepted projection 链路。

## 前端 app 下一步

- 补充真实文档加载、页码切换、块级对齐、翻译缓存和移动端布局。
- 保持三栏主界面，不做营销页。

## 管理后台下一步

- 在现有资料管理区增加“打开 Reader”入口。
- 管理 reader artifacts 的状态：已解析、待翻译、待解释、待审核。

## 后端下一步

- 新增 `/api/reader/documents`、`/api/reader/pages`、`/api/reader/translate`、`/api/reader/explain`。
- 复用现有 `/api/sources` 和 `/api/concepts/graph`，但不要直接改写 `wiki/concepts/*.md`。

## 数据库下一步

- 当前项目未引入数据库，先用文件 artifacts 保存阅读产物。
- 若未来接数据库，核心表应围绕 `document_id/page_id/block_id/translation_version/explanation_version` 设计。
